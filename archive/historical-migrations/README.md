# Historical migrations

This directory preserves one-off scripts that were used to transform historical
project data and are **not active tooling**.

Do not execute the scripts here. They are retained for audit and provenance
only; their assumptions may no longer match the current application data.
