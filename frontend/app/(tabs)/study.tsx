/**
 * Study tab — promotes the University hub into the bottom tab bar.
 * Re-export keeps a single source of truth (expo-router hidden-tabs pattern).
 */
export { default } from "../university/index";
