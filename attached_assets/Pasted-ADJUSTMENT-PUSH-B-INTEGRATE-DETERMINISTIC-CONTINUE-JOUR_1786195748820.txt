ADJUSTMENT PUSH B — INTEGRATE DETERMINISTIC CONTINUE JOURNEY

Use the newly added getJourneyRecommendation() selector.

Do not recreate this logic inside React components.

On the Journey/Book page:

1. Call the authoritative progression selectors first.

2. Convert their result into JourneyNodeUi objects.

3. Call:

getJourneyRecommendation(context)

4. Render exactly one primary CTA based on result.kind.

RESULT BEHAVIOR:

resume:
Button text = Resume Encounter
Navigate directly to result.href.

continue:
Button text = Continue Journey
Navigate to result.href.
Also mark result.nodeId as the one recommended Chapter node.

choose_branch:
Button text = Choose Shift
Open the EXISTING shift/branch selector.
Do not navigate directly to Day, Evening, or Night.

next_destination:
Navigate to the existing next progression destination.

complete:
Disable/hide primary continuation CTA according to existing completed-Book design.

RECOMMENDED NODE:

Only one node may receive:
data-recommended="true"

Do not compute recommended state independently inside individual map nodes.

The component should receive:

recommendedNodeId

and render the existing restrained teal/gold highlight.

Do not add another progression store.

Acceptance tests:

- active battle gets Resume Encounter
- Chapter 1 correctly gets Continue Journey
- Chapter 4 with Day + Evening available gets Choose Shift
- canonical Chapter 4 choice resumes selected route
- Chapter 7 does not arbitrarily choose one of three shifts
- completed Book follows existing next destination
- refresh produces identical recommendation

Commit:

feat(journey): integrate deterministic guided next action
Adjustment Push D — Lock explanations without duplicating gate logic

This is important. Replit should not write something like:

if (chapterNumber === 6) {
  return "Complete Chapter 5";
}

That would eventually drift away from your actual gating system.

Instead, the existing gate service should return both the Boolean and reason.

Use a result like:

export interface GateEvaluation {
  unlocked: boolean;

  unmetRequirements: {
    code: string;
    message: string;
  }[];
}

Examples from the gating layer:

{
  unlocked: false,
  unmetRequirements: [
    {
      code: "previous_chapter",
      message:
        "Complete Chapter 5 to continue.",
    },
  ],
}
{
  unlocked: false,
  unmetRequirements: [
    {
      code: "night_shift_locked",
      message:
        "Night Shift unlocks after completing Chapter 6.",
    },
  ],
}
{
  unlocked: false,
  unmetRequirements: [
    {
      code: "boss_keys_missing",
      message:
        "Collect 3 Chapter Boss Keys. Current progress: 2/3.",
    },
  ],
}

Then the UI is deliberately stupid:

type LockedNodeProps = {
  reasons: {
    code: string;
    message: string;
  }[];
};

export function LockReason({
  reasons,
}: LockedNodeProps) {
  if (reasons.length === 0) {
    return null;
  }

  return (
    <div
      role="status"
      className="journey-lock-reason"
    >
      {reasons.map((reason) => (
        <p key={reason.code}>
          {reason.message}
        </p>
      ))}
    </div>
  );