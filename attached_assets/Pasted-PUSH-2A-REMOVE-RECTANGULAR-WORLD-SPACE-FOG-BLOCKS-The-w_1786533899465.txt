PUSH 2A — REMOVE RECTANGULAR WORLD-SPACE FOG BLOCKS

The world-space fog architecture is now moving with the map correctly, but the visual implementation is still wrong.

CURRENT BUG:

Fog is being rendered as large rectangular blocks / canvases.

Visible examples:
- large dark navy rectangle
- lighter blue-gray rectangle
- rectangular scenic fog image on the right side

These rectangles move with MapWorld, but they still look like panels rather than atmospheric fog.

Goal:

Remove all rectangular fog-block rendering before adding final painted fog.

Do not change:
- camera
- map geometry
- player movement
- visibility radius
- explored/unexplored state
- encounter generation
- player sprite

Only change fog rendering.

AUDIT:

Locate all fog rendering code that creates:
- fixed-width rectangular divs
- fog quadrants
- fog chunks
- square canvas sections
- rectangular images
- fallback colored rectangles
- tile-grid fog blocks

Search specifically for:
- backgroundColor
- rectangular <div>
- <img> used as full block
- fogChunk
- fogRegion
- fogCell
- fogTile
- fogPanel
- mask rectangles

REMOVE:

Any fog element whose visible footprint is a rectangle.

Temporary state after this push:

It is acceptable for unexplored terrain to have NO final fog art temporarily.

Do not replace the rectangles with:
- circles
- gradients
- CSS blur
- square opacity masks

Keep the visibility data intact.

Architecture after removal:

<MapWorld>
  <ChapterEnvironment />
  <HexTerrain />
  <BossGate />
  <EncounterObjects />
  <PlayerSprite />

  <JourneyFogField />
</MapWorld>

JourneyFogField may temporarily render nothing visually.

The important goal is:

NO RECTANGULAR FOG ARTIFACTS.

Acceptance criteria:

1. No dark rectangular fog panels.
2. No blue-gray rectangular fog panels.
3. No scenic fog painting rectangle.
4. No tile-level fog rectangles.
5. Map still pans correctly.
6. Visibility state still updates internally.
7. Player/camera remain unchanged.

Commit:

fix(journey): remove rectangular fog blocks from world layer