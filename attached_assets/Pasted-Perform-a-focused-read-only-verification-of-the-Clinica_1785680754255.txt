Perform a focused read-only verification of the Clinica tutorial discovery audit. Do not modify application code.

Verify only the following issues:

1. Learner-profile identifiers

   * Locate every comparison or switch involving learning profiles.
   * List canonical and legacy identifiers.
   * Identify which systems fail to recognize `curious`, `nursing_student`, `nclex`, or `professional`.
   * Separate tutorial-depth bugs from Clinical Care behavior bugs.

2. Shop tutorial

   * Confirm whether `shopIntro` exists.
   * Identify its exact host screen, trigger, steps, required targets, persistence key, replay behavior, and completion behavior.
   * Determine whether it introduces the Shop Tab, Shop Hub, Apothecary Market, University Shop, or another shop.

3. Summoning tutorials

   * Separate the guaranteed recruitment ceremony from the normal Summoning Hall.
   * Confirm which screen uses `summonIntro`.
   * Confirm whether the normal 1-pull and 10-pull system has a first-use tutorial after the ceremony.

4. Team tutorial

   * Confirm whether `teamIntro` is triggered from Mission Loadout, Hall of Heroes, Hero Picker, or another screen.
   * List exactly what it teaches.
   * Determine whether the first mission loadout is adequately guided.

5. Tutorial persistence

   * Trace `useClearTutorialOnExit`.
   * Confirm whether leaving an incomplete tutorial marks it completed.
   * Confirm what happens on app closure, navigation replacement, hardware back, and tutorial replay.
   * Identify screens that use both `clinica.tutorials.v1` and `PlayerState seen_*` flags.

6. Currency terminology

   * Produce a table of every player-facing currency label and its internal CurrencyId.
   * Include every occurrence of Ward Coins, Gold Crowns, Crowns, Codex Shards, Summoning Shards, Hero Shards, Ward Sigils, Ward Tokens, Jade Scrolls, University Credits, Insight Crystals, Refined Lotus Gems, and Lotus Gems.
   * Mark each label as canonical, legacy, placeholder, contradictory, or unused.

7. First regular battle

   * Trace the exact first non-prologue battle available to a new player.
   * List the enemy, visible clues, concealed clues, available heroes, skills, Clinical Cue timing, locked actions, affinity results, and Care Chain requirements.
   * Do not include hidden formulas or internal resistance values.
   * Determine the exact order in which first-use tutorial prompts would trigger.

8. Audit contradictions

   * Resolve every contradiction found between the screen inventory, tutorial infrastructure section, and consolidated tutorial table.
   * Do not assume that a TutorialId means the tutorial is currently reachable.

Return:

A. Verified findings
B. Incorrect findings from the original audit
C. Findings that remain uncertain
D. Exact files and line ranges reviewed
E. Safe fixes required before tutorial implementation
F. Recommended first tutorial implementation push

Do not implement or edit tutorials during this task.
