Yes. In this screenshot, the MSK toggle is ON, and the huge black/white circular shapes are the legacy debug mask visualization. It is no longer representative of the production fog architecture we just moved to.
More importantly, it is still using the old geometric/radial logic, so even as a diagnostic it is misleading. We should not try to “tune” those circles. We should remove the old mask behavior and make the debug view visualize the same organic eraser field actually used by Base and Mid.
The production system should stay:
draw fog → directly destination-out with organic erasers → no intermediate mask canvas.
Use this corrective push.
CORRECTIVE PUSH — REMOVE LEGACY RADIAL MASK AND MAKE DEBUG VIEW MATCH ACTUAL ORGANIC FOG ERASURE

CURRENT SCREENSHOT DIAGNOSIS:

The MSK debug toggle is ON.

The huge black / white curved shapes currently visible are coming from
the legacy drawFogMaskDev / fogMask visualization.

This is NOT the intended production fog appearance.

The production fog system now uses direct destination-out erasure.

Therefore the old radial mask implementation is obsolete and must not
continue defining or visually suggesting the fog boundary.

==================================================
1. PRODUCTION MUST NOT USE A MASK CANVAS
==================================================

KEEP the current production architecture:

FogBase:
draw fog directly
→ destination-out organic erasure
→ source-over

FogMid:
draw fog directly
→ destination-out organic erasure
→ source-over

FogWisp:
sparse decorative fog

Do NOT restore:

destination-in
black/white mask compositing
offscreen production visibility mask
mask canvas applied to fog canvas

The production map must work identically whether MSK debug mode exists
or not.

==================================================
2. REMOVE LEGACY MASK SHAPE GENERATION
==================================================

Audit:

fogMask.ts
drawFogMaskDev
FogDevDiagnostic
MSK toggle rendering

The current debug mask appears to use large radial/circular gradients.

Delete or stop using any debug rendering that produces:

- giant circles
- large black crescents
- white radial spotlights
- hard geometric blobs
- old FOV radius circles

The debug mask must no longer call the old reveal algorithm.

==================================================
3. CREATE ONE SHARED ORGANIC REVEAL FUNCTION
==================================================

The exact SAME organic reveal generator should be used by:

FogBase direct erasure
FogMid direct erasure
MSK debug visualization

Create one canonical helper, for example:

buildOrganicRevealInfluences(...)

It should NOT draw immediately.

It should return deterministic lobe data.

Example:

interface OrganicRevealLobe {
  x: number;
  y: number;
  radius: number;
  strength: number;
}

function buildOrganicRevealInfluences({
  tile,
  tileSize,
  runSeed,
  state,
}): OrganicRevealLobe[]

For each visible/explored tile it returns:

- central soft influence
- 5–8 irregular secondary lobes
- deterministic seeded offsets
- different radii
- different strengths

No Math.random().

==================================================
4. BASE, MID, AND DEBUG MUST CONSUME SAME LOBES
==================================================

FogBase:

for each organic lobe:
eraseSoftLobe(...)

FogMid:

use the SAME lobe positions
but different erase strength/radius multiplier

MSK debug:

draw those SAME lobes into the debug visualization.

This guarantees the debug screen actually represents the production
visibility field.

==================================================
5. FOV HEXES MUST MERGE BEFORE THEY LOOK LIKE ART
==================================================

Gameplay:

FOV 1 =
current authored hex
+
all valid neighboring authored hexes

Current screenshot:
Visible Now = 6

That is valid near an authored map boundary.

But DO NOT visualize six separate radial areas.

All six visible tile organic influence clusters must overlap into ONE
continuous visibility region.

The player-visible clearing should have:

- irregular perimeter
- several protrusions
- several indentations
- soft feathering
- asymmetric boundary

It must NOT look like:

one giant circle

or:

six circles joined together

==================================================
6. CHANGE MSK DEBUG VISUALIZATION
==================================================

MSK should be diagnostic only.

Instead of mounting an opaque black/white canvas over the map, make it a
semi-transparent analytical overlay.

Recommended colors:

VISIBLE NOW:
transparent / very pale green
approximately 20% diagnostic opacity

EXPLORED:
amber or cyan
approximately 15–20%

UNEXPLORED:
dark blue
approximately 20–25%

The underlying map should remain visible while debugging.

Do NOT use:

solid black
solid white

This makes it much easier to understand whether the visibility field
matches the hex map.

==================================================
7. ADD TWO DEBUG MODES INSTEAD OF ONE CONFUSING MASK
==================================================

Replace current:

MSK

with either one cycling control or two small dev controls:

STATE
ALPHA

STATE:
shows logical tile classifications

VISIBLE NOW
EXPLORED
UNEXPLORED

This may use translucent tile tinting because it is developer-only.

ALPHA:
shows the actual organic eraser field produced by
buildOrganicRevealInfluences().

The ALPHA diagnostic should show:

clear/strong erase in visible center
partial erase in explored space
soft irregular transition

Neither diagnostic is production artwork.

==================================================
8. IMPORTANT — TILE STATE DEBUG CAN BE GEOMETRIC
==================================================

For development only:

STATE mode may color the actual hex cells.

For example:

green:
visibleNow

yellow:
explored

blue:
unexplored

This is useful because it confirms the GAMEPLAY calculation.

Then ALPHA mode separately confirms the VISUAL organic interpretation.

This cleanly separates:

HEX LOGIC
from
FOG ART

Do not mix them into one giant radial black/white debug surface.

==================================================
9. ORGANIC ERASURE PARAMETERS
==================================================

Use the previous approved behavior.

VISIBLE NOW:

Base erase strength:
0.97–1.00

Mid erase strength:
0.97–1.00

Strong central clearing.

EXPLORED:

Base erase strength:
0.60–0.75

Mid erase strength:
0.70–0.85

This preserves approximately 20–40% perceived memory haze.

UNEXPLORED:

no erasure.

==================================================
10. ORGANIC LOBE PARAMETERS
==================================================

Starting values:

main radius:
approximately tileSize × 1.05–1.25

secondary lobe count:
5–8

secondary offsets:

x:
-0.45R to +0.45R

y:
-0.40R to +0.40R

secondary radius:
0.40R to 0.85R

strength:
0.30–0.75 of parent strength

Use deterministic seed:

`${runSeed}:${tile.id}:fogReveal`

==================================================
11. OVERLAP IS REQUIRED
==================================================

Visible adjacent tiles should overlap significantly.

Do not treat each tile clearing as isolated.

After all visible tile influences are applied, there should be a broad
continuous center clearing.

The only noticeable boundary should be where the combined FOV reaches
unexplored territory.

==================================================
12. REMOVE OLD fogMask.ts RESPONSIBILITIES
==================================================

fogMask.ts may keep:

- cache key helpers
- debug types
- diagnostic utilities

But remove/deprecate any function that independently defines production
visibility geometry.

There must be ONE source of truth:

calculateVisibleTileIds()
for gameplay state

+

buildOrganicRevealInfluences()
for visual interpretation

==================================================
13. TEST WITH MSK OFF FIRST
==================================================

After implementation:

TEST 1:

B ON
M ON
W ON
MSK/STATE/ALPHA OFF

This is the REAL production appearance.

Verify:

- player area clear
- explored terrain lightly hazed
- unexplored terrain dense
- no giant circular reveal

Only after production view passes should debug visualization be tested.

==================================================
14. TEST LOGICAL STATE
==================================================

STATE ON

Verify:

Visible Now = 6 in this current edge position.

Those six highlighted authored hexes must match:

calculateVisibleTileIds()

No extra tile.

No missing valid tile.

==================================================
15. TEST ORGANIC ALPHA
==================================================

ALPHA ON

Verify:

the six visible cells merge visually into ONE irregular field.

There must be no:

- giant single circle
- black crescent
- white spotlight
- exact hex hole
- six-lobed flower

==================================================
16. ACCEPTANCE CRITERIA
==================================================

PASS only if:

[ ] production fog uses no mask canvas
[ ] direct destination-out remains
[ ] old radial drawFogMaskDev removed/deprecated
[ ] MSK no longer displays giant black/white circles
[ ] Base and Mid use same organic reveal topology
[ ] debug alpha uses the exact same topology
[ ] visible FOV cells merge into one organic clearing
[ ] explored region retains haze
[ ] unexplored remains dense
[ ] logical hex visibility can be debugged separately
[ ] production fog looks correct with all debug modes OFF

Commit:

refactor(journey): replace legacy radial mask debug with organic reveal diagnostics
The important distinction now
We actually have two completely different concepts that Replit has been mixing together:
Gameplay visibility
Which hexes can I see?
This can absolutely be clean hex mathematics.
And:
Fog silhouette
What does the mist look like around those visible hexes?
This should be irregular and painterly.
So I would stop calling the latter a “mask” in production code altogether. Call it something like:
OrganicRevealField
That will probably prevent Replit from repeatedly rebuilding a giant circular spotlight mask.
Also, judge the next screenshot with all diagnostic toggles off first. Right now the black/white shapes in this screenshot are the debug surface itself, so they obscure whether Base + Mid are finally producing the correct production result.