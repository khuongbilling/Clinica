name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: Route and journey-map validation tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'
          cache-dependency-path: frontend/yarn.lock

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Realm route integrity check
        run: node_modules/.bin/sucrase-node tests/realm_routes.test.ts

      - name: Route constant ↔ screen file cross-check
        run: node scripts/check-routes.js

      - name: Journey-map Stage 3 release contract
        run: npm run test:journey-stage-contract

      - name: Journey-map shift composition lock
        run: node_modules/.bin/sucrase-node tests/journey_map_shift_composition.test.ts
